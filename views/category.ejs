<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Groceries Inventory - <%= category.name %> Category</title>
  </head>
  <body>
    <h1><%= category.name %> Category</h1>
    <a href="/category/update/<%= category.id %>">Update category</a>

    <div>
      <h2>Items</h2>
      <a href="/item/new?categoryId=<%= category.id %>">Add new item</a>
      
      <% if (items.length === 0) { %>
        <p>No items were found.</p>
      <% } else { %>
        <ul>
          <% items.map(item => { %>
            <li><a href="/item/<%= item.id %>"><%= item.name %></a> - Quantity: <%= item.quantity %></li>
          <% }) %>
        </ul>
      <% } %>
    </div>
  </body>
</html>